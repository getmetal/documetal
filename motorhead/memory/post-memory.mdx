---
title: "Post Memory"
api: "POST https://api.getmetal.io/v1/motorhead/sessions/{sessionId}/memory"
description: "This endpoint creates a single memory."
---

### Params

<ParamField required path="sessionId" type="string">
  The Session ID
</ParamField>

### Body

<ParamField body="messages" type="object[]" required>
  <Expandable title="message-properties">
    <ResponseField name="role" type="string">
      Role of the message author
    </ResponseField>
    <ResponseField name="content" type="string">
      Content of the message
    </ResponseField>
  Index ID
</ParamField>

<ParamField body="body" type="string">
  Incremental summarization of the conversation
</ParamField>

### Response

<ResponseField name="data" type="Memory Object">
  <Expandable title="properties">
    <ResponseField name="messages" type="array">
        <Expandable title="message-properties">
            <ResponseField name="role" type="string">
                Role of the message author
            </ResponseField>
            <ResponseField name="content" type="string">
                Content of the message
            </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="context" type="string">
      Incremental summarization of the conversation
    </ResponseField>

  </Expandable>
</ResponseField>

<RequestExample>

```bash Example Request
curl --location --request POST 'https://api.getmetal.io/v1/motorhead/sessions/{sessionId}/memory' \
--header 'Content-Type: application/json' \
--data-raw '{
    "messages": [
      { "role": "Human", "content": "Who is the best vocalist of all time?" },
      { "role": "AI", "content": "Ozzy!" }
    ],
    "context": "User ask what can he eat in Colombia. The AI responds arepas are really nice"
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": {
    "messages": [
      { "role": "Human", "content": "Who is the best vocalist of all time?" },
      { "role": "AI", "content": "Ozzy!" }
    ],
    "context": "User ask what can he eat in Colombia. The AI responds arepas are really nice"
  }
}
```

</ResponseExample>
