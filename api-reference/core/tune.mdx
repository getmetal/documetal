---
title: "/tune"
api: "POST https://api.getmetal.io/v1/tune"
description: "Tune your embeddings for better results."
---

### Auth Headers

<ParamField required header="x-metal-api-key" type="string">
  An API key for your org.
</ParamField>

<ParamField required header="x-metal-client-id" type="string">
  A Client ID for your org.
</ParamField>

### Body

<ParamField body="app" type="string" placeholder="App ID" required>
  App ID
</ParamField>

<ParamField body="idA" type="string" placeholder="Embedding ID A" required>
  Embedding ID A
</ParamField>

<ParamField body="idB" type="string" placeholder="Embedding ID B" required>
  Embedding ID B
</ParamField>

<ParamField
  body="result"
  type="string"
  placeholder="Score result. Either -1 or 1"
  required
>
  Score result. Either -1 or 1
</ParamField>

### Response

<ResponseField name="data" type="Tuning Object">
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Id of the Tuning
    </ResponseField>

    <ResponseField name="app" type="string">
      Id of the app for the Tuning
    </ResponseField>

    <ResponseField name="idA" type="string">
     Embedding ID A
    </ResponseField>

    <ResponseField name="idB" type="string">
      Embedding ID B
    </ResponseField>

    <ResponseField name="result" type="string">
      The score result. Either `-1` or `1`
    </ResponseField>

  </Expandable>
</ResponseField>

<RequestExample>

```bash Example Request
curl --location --request POST 'https://api.getmetal.io/v1/tune' \
--header 'Content-Type: application/json' \
--header 'x-metal-api-key: <api-key>' \
--header 'x-metal-client-id: <client-id>' \
--data-raw '{
    "app": "app-id",
    "idA": "embedding-1",
    "idB": "embedding-2",
    "results": '-1'
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": {
    "id": "1",
    "app": "app-id",
    "idA": "embedding-1",
    "idB": "embedding-2",
    "result": "-1"
  }
}
```

</ResponseExample>
